---
tags: [frontend, seo, stable]
updated: 2026-05-21
---

# SEO & Metadata

## Site config

`src/lib/site.ts` (`siteConfig`) is the **single source of truth** for SEO —
name, description, origin URL, OG image, Twitter handle, theme colour. The
metadata generator, `robots.ts`, `sitemap.ts`, and the JSON-LD helper all read
from it. **Update the placeholder values per project.** `#todo`

`siteConfig.url` comes from `NEXT_PUBLIC_SITE_URL` (see [[environment-variables]]),
falling back to `http://localhost:3000`.

## Metadata generator

`src/utils/seo/generate-page-metadata.ts` exports two builders:

```ts
import {
  generateMetadata,
  generateViewport,
} from "@/utils/seo/generate-page-metadata";

export const metadata = generateMetadata({ title: "…", description: "…" });
export const viewport = generateViewport();
```

- `generateMetadata(props?)` → a Next.js `Metadata` object: title/description,
  OpenGraph, Twitter cards, canonical, icons, robots. `metadataBase` is **always
  set** from `siteConfig.url`, so relative URLs (OG image, canonical) resolve to
  absolute — required by social scrapers.
- `generateViewport()` → the `Viewport` export. `themeColor` lives here, **not**
  on the metadata object (Next deprecated it there).

Accepted `generateMetadata` options: `title`, `description`, `url`, `ogImage`,
`twitterHandle`, `author`, `siteName`. (`keywords` and the non-standard `other`
tags were dropped — no SEO value.)

## robots.txt & sitemap.xml

Generated by the Next file conventions:
- `src/app/robots.ts` → `/robots.txt` — allows all crawlers, disallows `/admin`
  and `/api/`, points at the sitemap.
- `src/app/sitemap.ts` → `/sitemap.xml` — the home route plus one entry per
  **active** product, read from [[catalog-store]] and regenerated hourly
  (`export const revalidate = 3600`).

> [!bug] `/sitemap.xml` is **currently 404 on the deployment** — unresolved
> Three implementations, all correct locally, all 404 once deployed:
>
> | Attempt | dev | `next start` | Deployed |
> |---|---|---|---|
> | `sitemap.ts`, static | ✅ | ✅ | ✅ but built before the catalogue existed — home only |
> | `sitemap.ts` + `force-dynamic` | ✅ | ✅ | **404** |
> | `app/sitemap.xml/route.ts` | ✅ | ✅ | **404** |
> | `sitemap.ts` + `revalidate = 3600` (current) | ✅ | ✅ 41 URLs | **404** |
>
> What the deployment says: `X-Matched-Path: /404`, so Vercel's router has no
> entry for the path at all — this is not the handler failing, it is the route
> not existing in what is being served.
>
> **What is ruled out.** "Vercel resolves paths with a file extension against the
> static filesystem first" — I wrote that here and it is wrong: `/robots.txt`
> comes from the same metadata convention, carries an extension, and answers 200
> with `X-Matched-Path: /robots.txt`.
>
> Also ruled out: **that the build is failing**. Changing `NEXT_PUBLIC_SITE_URL`
> and redeploying propagated to the canonical tag, `og:url`, `og:image`, the
> JSON-LD and `robots.txt` within minutes — that value is inlined at build time,
> so a new build demonstrably succeeded and shipped. `/sitemap.xml` stayed 404
> through it.
>
> **Next thing to check**, and it needs the dashboard: open the deployment's
> build log and find the routes table. If `○ /sitemap.xml` is listed there — it
> is listed in a local `npm run build` — the route exists in the output and this
> is a routing or edge-cache problem, not a generation one. Every request,
> including cache-busting query strings and `/sitemap`, comes back
> `X-Vercel-Cache: HIT` with `X-Matched-Path: /404`, which is consistent with a
> cached 404 that never reaches the origin. Purging the cache is the cheap thing
> to try before touching the code again.
>
> Whatever the cause: verify against a **deployment**, never `next start`. Every
> one of these passed `next start`.

## Structured data (JSON-LD)

`src/utils/seo/structured-data.ts` → `getSiteStructuredData()` builds an
`Organization` + `WebSite` JSON-LD graph. The root layout renders it once in a
`<script type="application/ld+json">`.

## Bot detection

`src/utils/is-bot.ts` exports `isBot()` — a **server-only** async helper that
reads the `user-agent` header and returns `true` for Lighthouse, Googlebot,
PageSpeed, HeadlessChrome, GTmetrix, Pingdom, Bingbot, Yandexbot.

> [!caution]
> Calling `isBot()` (or any `headers()` reader) in a route **opts it out of
> static prerendering** — a TTFB cost for every visitor. Serving crawler-specific
> behaviour also borders on cloaking. Prefer the [[animation-system|reduced-motion]]
> path (`<ReducedMotion>`): animations only touch opacity/transform, so content
> is always in the DOM and crawlers see it regardless. Use `isBot()` sparingly.

## Static assets

The `public/` **root** holds meta/PWA/SEO assets — favicons (multiple sizes),
Android/Apple icons, `manifest.json`, `browserconfig.xml`, `open-graph.png`.
Site **content** assets (images, videos) go under `public/assets/<section>/` —
see [[folder-structure]].

> [!note] `#todo`
> `open-graph.png` is currently **900×600** (the metadata declares the same, so
> there is no mismatch). The ideal OG size is **1200×630** — swap in a
> correctly-sized asset and update the dimensions in `generate-page-metadata.ts`.

## Related

[[routing]] · [[utils]] · [[animation-system]] · [[environment-variables]] · [[decisions-log]]
